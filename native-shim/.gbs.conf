[general]
profile = profile.contextengine

# TODO: 아래는 자리표시자 — 회사 내부 Tizen OBS/패키지 저장소 URL로 교체.
# 이 파일이 없어도 `gbs build`에 `--repository <url>`로 넘길 수 있음.
[profile.contextengine]
repos = repo.base
buildroot = /home/abuild

[repo.base]
url = https://example.invalid/tizen/repo  # TODO: 실제 저장소 URL로 교체
